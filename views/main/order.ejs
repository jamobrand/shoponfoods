<%- layout('layout') -%>

<script>
  const d = 200;
  const e = 600;
</script>


<div class="container">

  <div class="row">

    <!--<div class="col-md-8 order-md-2 mb-4  checkme">
      <h4 class="d-flex justify-content-between align-items-center mb-3">
        <span class="text-muted">Your Address Details</span>

      </h4>

      <ul class="list-group mb-3">

        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <div>
            <h6 class="my-0">Full Names</h6>
          </div>
          <span class="text-muted">James Kiama</span>
        </li>
        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <div>
            <h6 class="my-0">Email</h6>
          </div>
          <span class="text-muted">James@gmail.com</span>
        </li>
        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <div>
            <h6 class="my-0">Phone Number</h6>
          </div>
          <span class="text-muted">0718765890</span>
        </li>
        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <div>
            <h6 class="my-0">Delivery Area</h6>
          </div>
          <span class="text-muted">Ngong</span>
        </li>
        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <div>
            <h6 class="my-0">Landmark</h6>
          </div>
          <span class="text-muted">Kambwe Flats</span>
        </li>
        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <div>
            <h6 class="my-0">Special Info</h6>
          </div>
          <span class="text-muted">Call Me</span>
        </li>


      </ul>
    </div>-->


    <div class="col-md-8 order-md-2 mb-4 checkme">
      <h4 class="d-flex justify-content-between align-items-center mb-3">
        <span class="text-muted">Your Shopping Basket</span>
      </h4>

      <ul class="list-group mb-3">

        <form method="post" action="/order">
          <% for (var i = 0; i < foundCart.items.length; i++) { %>
        <li class="list-group-item d-flex justify-content-between lh-condensed">
          <img class="order-image-my-gig" src="<%= foundCart.items[i].item.image %>" alt="" >
          <div>
            <input type="hidden" name="title" value="<%= foundCart.items[i].item.title %>"><%= foundCart.items[i].item.title %></input>
            <!--<h6 class="my-0" name="title"><%= foundCart.items[i].item.title %></h6>
            <small class="text-muted">Brief description</small>-->
          </div>
          <input type="hidden" value="<%= foundCart.items[i].price %>">KSh <%= foundCart.items[i].price %></input>
          <!--<span class="text-muted" name="paid">KSh <%= foundCart.items[i].price %></span>-->

        </li>
        <% } %>
        <li class="list-group-item d-flex justify-content-between">
          <span>Sub-Total :</span>
          <input type="hidden" name="total" value="<%= foundCart.total %>"><%= foundCart.total %></input>
          <!--<strong name="total"><%= foundCart.total %></strong>-->
        </li>

        <li class="list-group-item d-flex justify-content-between">
          <span>Delivery :</span>

            <% if( foundCart.total < 1000) { %>
             200
            <% } else { %>
             600
            <% } %>

        </li>
        <li class="list-group-item d-flex justify-content-between">
          <span>Total (KSH)</span>
          <% if( foundCart.total < 1000) { %>
            (200 + <%= foundCart.total %>)
          <% } else { %>
          <strong>600</strong> + <strong><%= foundCart.total %></strong>
          <% } %>
        </li>

         <br>
        <button class="btn btn-primary btn-lg btn-block" type="submit">Place Order</button>
      </form>
      </ul>
    </div>

  </div>
</div>
